#define DEF_IDLECHAIN_VERSION_TAG "@VERSIONTAG@"
#define DEF_IDLECHAIN_VERSION "1.0.0"
#define DEF_IDLECHAIN_RELEASE_NAME "So IDLE"
#define DEF_IDLECHAIN_VERSION_FULL DEF_IDLECHAIN_VERSION "-" DEF_IDLECHAIN_VERSION_TAG
#define DEF_IDLECHAIN_VERSION_IS_RELEASE @VERSION_IS_RELEASE@

#include "version.h"

const char* const IDLECHAIN_VERSION_TAG = DEF_IDLECHAIN_VERSION_TAG;
const char* const IDLECHAIN_VERSION = DEF_IDLECHAIN_VERSION;
const char* const IDLECHAIN_RELEASE_NAME = DEF_IDLECHAIN_RELEASE_NAME;
const char* const IDLECHAIN_VERSION_FULL = DEF_IDLECHAIN_VERSION_FULL;
const bool IDLECHAIN_VERSION_IS_RELEASE = DEF_IDLECHAIN_VERSION_IS_RELEASE;
